name: keep-alive
on: 
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
  
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping
        run: |
          curl -f https://seu-servidor.onrender.com/keep-alive || exit 1
